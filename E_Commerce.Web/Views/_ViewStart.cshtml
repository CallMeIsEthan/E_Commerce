@{
    // Root level views không dùng layout mặc định
    // Area User sẽ dùng _UserLayout (đã set trong Areas/User/Views/_ViewStart.cshtml)
    // Area Admin sẽ dùng _AdminLayout (đã set trong Areas/Admin/Views/_ViewStart.cshtml)
    Layout = null;
}
